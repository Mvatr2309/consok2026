# Инструкция по пользованию сервисом «МФТИ Консультации»

**Адрес сервиса:** https://miptconsultations.icust.online

---

## 1. Для абитуриентов (запись на консультацию)

### Как записаться

1. Перейдите по ссылке на нужную программу (ссылку предоставляет администратор или можно выбрать программу на главной странице).
2. Выберите эксперта из списка.
3. Выберите удобный слот (дата и время). Отображаются все будущие слоты, у которых есть свободные места.
4. Введите ваше **имя** и **email**.
5. Нажмите **«Записаться»**.
6. После успешной записи вы увидите:
   - Ссылку на встречу
   - Кнопку **«Добавить в Google Календарь»**
   - Кнопку **«Скачать .ics»** (для Яндекс Календаря, Apple Calendar и других)
   - Кнопку **«Напоминание в Telegram»** — подписка на напоминание через Telegram-бота
7. На указанный email придёт письмо с подтверждением и деталями встречи.
8. За 1 час до встречи придёт напоминание на email и в Telegram (если подписаны).
9. При отмене консультации администратором вы получите уведомление на email и в Telegram.

### Ссылки на программы

| Программа | Ссылка |
|-----------|--------|
| Науки о данных | https://miptconsultations.icust.online/programs/1 |
| Разработка ИТ-продукта | https://miptconsultations.icust.online/programs/2 |
| Управление ИТ-продуктами | https://miptconsultations.icust.online/programs/3 |

Главная страница со всеми программами: https://miptconsultations.icust.online

---

## 2. Для экспертов (личный кабинет)

### Вход в кабинет

1. Перейдите на https://miptconsultations.icust.online/expert
2. Введите **логин** и **пароль**, выданные администратором.
3. Нажмите **«Войти»**.

### Возможности кабинета

- **Предстоящие консультации** — список ваших будущих слотов с информацией о записавшихся (имя, email, дата записи). Отображается по 10 штук с пагинацией.
- **Отметка присутствия** — в столбце «Присутствие» можно отметить чекбоксом, пришёл ли абитуриент на консультацию. Доступно как в списке, так и в модальном окне календарного вида.
- **Архив** — прошедшие встречи с историей записей.
- **Ссылка на встречу** — отображается вверху кабинета.
- **Выход** — кнопка «Выйти» в правом верхнем углу.

### Учётные данные экспертов

| Эксперт | Логин | Пароль |
|---------|-------|--------|
| Вадим Кирсанов | kirsanov | Consul#Mfti7x |
| Владислав Дежинин | dezhinin | Zapis!Expert4k |
| Алёна Читадзе | chitadze | Grupp@Meet9w |
| Дмитрий Пелик | pelik | Sloty&Phys3j |

---

## 3. Для администратора (админ-панель)

### Вход

1. Перейдите на https://miptconsultations.icust.online/admin
2. Введите логин и пароль HTTP Basic Auth (заданы в переменных окружения `ADMIN_USER` и `ADMIN_PASS`).

### Вкладки админ-панели

#### Программы
- Просмотр, создание, редактирование и удаление образовательных программ.
- Привязка экспертов к программам.
- Столбец **«Ссылка для абитуриентов»** — готовая ссылка на страницу записи, с кнопкой «Копировать».

#### Эксперты
- Просмотр, создание, редактирование и удаление экспертов.
- Загрузка фото эксперта.
- Указание ссылки на встречу (Google Meet / Zoom / др.).
- Привязка к программам.
- Задание **логина и пароля** для входа в личный кабинет (раздел «Авторизация в кабинет» в форме редактирования). При редактировании оставьте поле пароля пустым, чтобы не менять текущий.
- Столбец **«Логин»** — текущий логин эксперта.
- Столбец **«Кабинет эксперта»** — прямая ссылка на кабинет (по токену), с кнопкой «Копировать».

#### Слоты
- Просмотр всех слотов с информацией: дата/время, программа, эксперт, занятость мест, ссылка на встречу.
- **Пагинация**: отображается по 10 слотов на страницу с навигацией (в табличном виде).
- **Два вида отображения**: таблица и календарь (переключение кнопками «Таблица» / «Календарь»).
- **Создание** слотов вручную (кнопка «+ Добавить»).
- **Импорт из CSV** — массовое создание слотов:
  1. Нажмите «Скачать шаблон CSV» для получения шаблона.
  2. Заполните файл: `Эксперт;Программа;Дата и время (ГГГГ-ММ-ДД ЧЧ:ММ);Макс. участников`
  3. Нажмите «Загрузить CSV».
- **Массовое удаление**:
  1. Отметьте чекбоксами нужные слоты (или «Выделить все» в заголовке).
  2. Нажмите **«Удалить выбранные (N)»**.
  3. Подтвердите удаление. Связанные записи (букинги) также будут удалены.
- Редактирование и удаление отдельных слотов.

#### Записи
- Просмотр всех записей абитуриентов: имя, email, программа, эксперт, дата слота, дата записи.
- **Пагинация**: отображается по 10 записей на страницу с навигацией.
- Удаление записей — при удалении абитуриенту приходит уведомление об отмене на email и в Telegram (если подписан).

#### Согласия
- Просмотр логов согласий пользователей (персональные данные, запись встречи, cookie).

#### Уведомления
- **Письмо при регистрации** — включить/выключить автоматическую отправку письма-подтверждения при записи.
- **Напоминание перед встречей** — включить/выключить и настроить за сколько минут до встречи отправлять напоминание (по умолчанию 60 минут). Напоминания включены по умолчанию — если настройка не задана явно, система считает их включёнными.

---

## 4. Telegram-бот (@mipt_consultation_bot)

### Назначение
Бот отправляет напоминания о консультациях в Telegram. Работает через long polling.

### Как подписаться
1. После записи на консультацию нажмите кнопку **«Напоминание в Telegram»** на странице успешной записи.
2. Откроется Telegram с ботом — нажмите **«Запустить»** (Start).
3. Бот подтвердит подписку и покажет:
   - Детали консультации (программа, эксперт, дата/время)
   - Ссылку на встречу
   - Кнопки «Google Календарь» и «Скачать .ics»
4. За 1 час до встречи придёт напоминание со ссылкой на встречу.

### Защита от спама
Бот реагирует **только** на команду `/start` с deep link (от кнопки на сайте). Произвольные сообщения игнорируются.

### Уведомление об отмене
Если администратор удаляет запись, подписчик получает сообщение об отмене в Telegram.

### Управление на сервере
```bash
# Статус
pm2 status telegram-bot

# Перезапуск
pm2 restart telegram-bot

# Логи
pm2 logs telegram-bot
```

---

## 5. Техническая информация

### Сервер
- **IP:** 195.209.219.226
- **Домен:** miptconsultations.icust.online
- **Порт приложения:** 3001
- **Процесс:** PM2 (имя: `consultations`)
- **SSL:** Let's Encrypt (автопродление)

### Управление сервером

```bash
# Подключение
ssh -i ~/Downloads/vkrplatform-053038-digitalmipt.pem ubuntu@195.209.219.226

# Статус приложения
pm2 status

# Перезапуск
pm2 restart consultations

# Логи
pm2 logs consultations

# Логи рассылки напоминаний
cat /home/ubuntu/consultations/reminders.log
```

### Обновление на сервере

```bash
cd /home/ubuntu/consultations
git pull origin main
npx prisma migrate deploy    # если есть новые миграции
npx prisma generate
npm run build
pm2 restart consultations
```

### База данных
- **PostgreSQL 16** на localhost:5432
- БД: `consultations`, пользователь: `consultations`

### Cron-задачи
- Напоминания проверяются каждые 5 минут: `*/5 * * * *`

### Стек
- Next.js 16 (App Router), TypeScript
- Prisma 7 + PostgreSQL
- CSS Modules
- Nodemailer (SMTP: digital@phystech.edu)
- PM2, Nginx, Let's Encrypt

### Репозиторий
https://github.com/Mvatr2309/consok2026
